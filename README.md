#How to Run Tests

* Install all dependencies
 `pip install pipenv`
 `pipenv install requests`
 `pipenv install json`
 `pipenv install jsonpath_ng`
 `pipenv install pytest`

* Run All Tests
`pipenv shell`
`pytest -v tests`